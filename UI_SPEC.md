# MuMupow — UI Specification (for redesign)

เอกสารนี้อธิบายหน้าตา/โครงสร้าง/พฤติกรรมของ UI โปรแกรม MuMupow ทั้งหมด สำหรับส่งให้ทีมออกแบบ (Claude design) ออกแบบใหม่ แล้วนำกลับมาพัฒนาต่อ

> เทคโนโลยีปัจจุบัน: Python + Tkinter (custom dark theme, ไม่ใช้ theme library) · Windows only · แอปตัวเดียว หน้าต่างเดียว

---

## 1. ภาพรวมโปรแกรม

MuMupow = โปรแกรมเดสก์ท็อปสำหรับ **สั่งงานอัตโนมัติ (บอท) บน MuMu Player (Android emulator) หลายจอพร้อมกัน** ควบคุมผ่าน ADB

**ผู้ใช้ 2 กลุ่ม:**
- **ลูกค้า (ผู้ใช้ทั่วไป):** แค่เลือกจอ → เลือกสคริปต์ → กดรัน → ดูผล (ไม่เก่งคอม อยากให้ง่าย)
- **ผู้สร้างสคริปต์ (เจ้าของ):** สร้าง/แก้สคริปต์ ตั้งพิกัด/ภาพ anchor ปรับ config

**งานหลักที่ผู้ใช้ทำ (flow จริง):**
1. เปิดโปรแกรม → ดูว่า **MuMu เชื่อมต่อครบทุกจอยัง** + ดู **รายชื่อรหัส (บัญชี)** ว่าจะทำรหัสไหน
2. ไป **หน้าแรก** → เลือกสคริปต์ → กดรัน → ดูความคืบหน้าต่อจอ
3. รันวนทีละรหัสต่อจอ (ล็อกอิน → ทำงาน เช่น เก็บของในเกม → ออก → รหัสถัดไป) 10 จอ 50+ รหัส
4. รันทิ้งไว้ยาวๆ ไม่มีคนเฝ้า → ถ้าจอไหนติด อยากรู้ว่าติดตรงไหน

---

## 2. Window / App Shell

- ขนาดเริ่มต้น **1280 × 760 px** · ขั้นต่ำ **1180 × 720 px** · ปรับขนาดได้
- Title: `MuMupow - โปรแกรมควบคุมและบอทจำลองหน้าจอพร้อมกันหลายจอ`
- โครงจากบนลงล่าง:
  1. **Header bar** (สูง 58px)
  2. **Status bar** (สูง 34px)
  3. **Content** = Sidebar ซ้าย (กว้าง 260px) + Tab panel ขวา (ยืดเต็ม)
  4. **Log panel** ล่างสุด (ยุบ/ขยายได้)

---

## 3. Design Tokens (ค่าจริงปัจจุบัน)

### สี (Dark theme)
| ชื่อ | Hex | ใช้กับ |
|------|-----|--------|
| BG_DARK | `#080D16` | พื้นหลังหลัก/แท็บ |
| BG_PANEL | `#0C1422` | header, sidebar, status bar, การ์ดปุ่มรัน |
| BG_CARD | `#0D1523` | การ์ด/พาเนลเนื้อหา |
| BG_SURFACE | `#121D2E` | แถวรายการ (บัญชี/การ์ด) |
| BG_INPUT | `#0A111D` | ช่องกรอก/ลิสต์ |
| BG_HOVER | `#18243A` | hover |
| FG_WHITE | `#E6EDF7` | ตัวอักษรหลัก |
| FG_MUTED | `#8DA0B8` | ตัวอักษรรอง/คำอธิบาย |
| FG_DIM | `#64748B` | ตัวอักษรจาง |
| LINE_SOFT | `#223046` | เส้นขอบ |
| ACCENT_BLUE | `#38BDF8` | ไฮไลท์/หัวข้อฟอร์ม |
| ACCENT_GREEN | `#0F766E` | ปุ่มหลัก/สถานะพร้อม/สำเร็จ |
| ACCENT_RED | `#7F1D1D` | ปุ่มอันตราย/ลบ/หยุด/error |
| ACCENT_ORANGE | `#A16207` | เตือน/สถานะไม่ครบ |

### ปุ่ม (Button variants) — สไตล์ flat, มุมไม่มน (relief flat, bd 0), cursor มือ, hover เปลี่ยนสี
| variant | bg | hover | ใช้กับ |
|---------|-----|-------|--------|
| primary | `#0F766E` | `#115E59` | ปุ่มหลัก (รัน/บันทึก) |
| accent | `#0F2F4A` | `#164E72` | ปุ่มรอง (สแกน/เชื่อมต่อ) |
| danger | `#7F1D1D` | `#991B1B` | ลบ/หยุด |
| warning | `#A16207` | `#854D0E` | ทดสอบ/สร้างสคริปต์เร็ว |
| subtle | `#121D2E` | `#18243A` | ปุ่มเสริม |
| neutral | `#18243A` | `#24344B` | ทั่วไป |

### ฟอนต์
- หลัก: **Segoe UI** (10–16px, มี bold/italic) · ตัวอักษรลิสต์/พิกัด: **Consolas** (9–10px, monospace)
- หัวข้อใหญ่ 16 bold · หัวข้อพาเนล 12 bold · เนื้อหา 10 · คำอธิบาย 9 italic muted

### สไตล์รวม
- ธีมมืดน้ำเงินเข้ม (navy) · การ์ดขอบบาง 1px สี LINE_SOFT · ไม่มี gradient/เงา · ใช้ emoji เป็นไอคอนนำหน้าปุ่ม/หัวข้อ · ภาษาไทยทั้งหมด (โค้ด/คอมเมนต์อังกฤษ)

---

## 4. Header bar (บนสุด, BG_PANEL, สูง 58)
- ซ้าย: โลโก้ตัวอักษร **"MuMupow"** (16 bold ขาว) + subtitle *"Operational console for multi-emulator automation"* (10 muted) — **หมายเหตุ: subtitle เป็นอังกฤษ ขัดกับ UI ไทย ควรแก้/ตัด**

## 5. Status bar (ใต้ header, BG_PANEL, สูง 34)
- ซ้าย: สรุปสถานะ เช่น `Emulator: 1/1 | Accounts: 1/63 | Steps: 13 | Profile: Default Login | Status: Ready`
- ขวา: ข้อความช่วยเหลือ `เลือก Emulator + บัญชี + มาโคร แล้วกดรัน` (**ซ้ำกับ 3 สเต็ปในหน้าแรก อาจตัด**)

## 6. Sidebar ซ้าย (BG_PANEL, กว้าง 260, เห็นทุกแท็บ) — จัดการจอ
1. หัวข้อ **"จัดการอุปกรณ์ Emulator"** (12 bold)
2. ปุ่ม **"สแกนพอร์ต"** (accent, เต็มกว้าง)
3. **ป้ายสถานะเชื่อมต่อ:** `🟢 เชื่อมต่อ N จอ (เลือก M)` เขียวถ้าเจอจอ / `🔌 ยังไม่พบจอ — กดสแกนพอร์ต` ส้มถ้าไม่เจอ
4. กล่อง **"เชื่อมต่อด้วย IP:Port เอง"**: ช่องกรอก (ดีฟอลต์ `127.0.0.1:7555`) + ปุ่ม "เชื่อมต่อ" (primary)
5. หัวข้อ **"เลือก Emulator ที่จะควบคุม:"**
6. **ลิสต์จอ** (scrollable): แต่ละแถว = checkbox + `127.0.0.1:<port>` + ปุ่ม **X แดง** (ลบจอออกจากลิสต์ — **ความหมายไม่ชัด ควรมี tooltip**)

---

## 7. Tab panel ขวา (5 แท็บ เรียงตาม flow การใช้งาน)

### 7.1 🏠 หน้าแรก (รัน) — landing page
แบ่ง 2 คอลัมน์:
- **ซ้าย (การ์ดรัน):**
  1. หัวข้อ "เลือกสคริปต์ที่จะรัน" + คำใบ้ "1) ติ๊กจอทางซ้าย 2) เลือกสคริปต์ 3) กดรัน"
  2. **Dropdown เลือกสคริปต์** (readonly combobox)
  3. **การ์ด "พร้อมรันไหม?"** (BG_PANEL): 3 บรรทัดเช็คลิสต์ — `✓/✗ จอที่เลือก: N จอ` · `✓/✗ บัญชีที่จะทำ: N รหัส` · `✓/✗ สคริปต์: <ชื่อ> · N สเต็ป` + บรรทัดเตือน (`⚠️ ...` ส้ม / `✅ พร้อมรันแล้ว` เขียว) — อัปเดตสดตามการติ๊ก
  4. **การ์ดตัวเลือกการรัน** (ปักล่าง): `🖼️ ความถี่เช็ค Anchor (วิ)` [ช่องกรอก 0.5] + คำอธิบาย · checkbox `⏸️ หยุดรอตรวจทานทีละชุด`
  5. **ปุ่ม ▶ รัน** (primary, ใหญ่ สูง 2 แถว) + **ปุ่ม ■ หยุดทันที** (danger, ใหญ่ ปิดใช้ตอนไม่รัน)
- **ขวา (ความคืบหน้าต่อจอ):**
  1. หัวข้อ "ความคืบหน้าต่อจอ" + คำอธิบาย
  2. ปุ่ม **"📁 เปิดรายงานปัญหา (ภาพจุดที่ติด)"** (subtle)
  3. **การ์ดต่อจอ** (อัปเดตสดทุก 1.2–2.5 วิ): ไอคอนสถานะ (🟢 กำลังรัน / ⏳ / ⚪ รอ) + ชื่อจอ + ชื่อบัญชีที่กำลังทำ

### 7.2 👥 บัญชี — จัดการรหัส
แบ่ง 2 คอลัมน์:
- **ซ้าย (ลิสต์บัญชี):**
  1. **ช่องค้นหา**
  2. **แถบ batch:** ปุ่ม "ลบที่เลือก" (danger) · "ย้ายกลุ่มไป:" [ช่องกรอกกลุ่ม] + ปุ่ม "ย้ายกลุ่ม" (accent)
  3. **แถบเลือกด่วน:** "เลือกด่วน:" + ปุ่ม `🔴 ที่พลาด` · `⚪ ที่ยังไม่เสร็จ` + legend `🟢เสร็จ 🔴พลาด ⚪ยังไม่ทำ`
  4. **ลิสต์บัญชี** (scrollable) จัดเป็น **กลุ่ม**: หัวกลุ่ม = checkbox `📂 กลุ่ม: <ชื่อ> (N บัญชี)` + ปุ่ม "🗑️ ลบกลุ่ม" · แต่ละบัญชี (BG_SURFACE) = **จุดสถานะ** (🟢/🔴/🟡/⚪) + checkbox + ข้อความสรุป (ชื่อ · กลุ่ม · token ย่อ) + ปุ่ม "📝" แก้ + ปุ่ม "X" ลบ
- **ขวา (ฟอร์มเพิ่ม/แก้ไขบัญชี):** ช่องกรอก อีเมล/ไอดีเกม · ชื่อเรียก · รหัสผ่าน · กลุ่ม · โทเคน (OAuth2) · ไคลเอนต์ไอดี + ปุ่ม "เพิ่มบัญชีเข้าคิว" (primary) · "นำเข้าบัญชีแบบกลุ่ม" (accent) · "Import Save Web Game JSON" + กล่อง "คู่มือบัญชี"

### 7.3 ✏️ สร้าง/แก้สคริปต์ — step editor (สำหรับผู้สร้าง)
แบ่ง 2 คอลัมน์:
- **ซ้าย (จัดการสคริปต์ + ลิสต์ขั้นตอน):**
  1. คำใบ้ "แก้ไขสคริปต์ที่โหลดอยู่ (เลือกสคริปต์ได้ที่หน้าแรก)"
  2. "ชื่อโปรไฟล์:" [ช่องกรอก]
  3. แถวปุ่ม: "บันทึก" (primary) · "ลบ" (danger) · "Export" · "Import"
  4. แถวปุ่ม: "สร้างสคริปต์เร็ว" (warning) · "จัดการ Sets" (accent) · **"🔀 ผังงาน"** (primary)
  5. **ลิสต์ขั้นตอน** (Listbox, Consolas): แต่ละบรรทัด = `NN <ไอคอน anchor 🎯/📷> <ชนิด> <รายละเอียด> <delay> <desc>`
  6. แถวปุ่ม: "▲ ขึ้น" · "▼ ลง" · "ลบขั้นตอน" (danger)
- **ขวา (ฟอร์มแก้ขั้นตอน, scrollable):**
  1. กล่อง "➕ เพิ่ม / แก้ไขขั้นตอนคำสั่ง"
  2. ช่อง: ประเภทคำสั่ง (dropdown) · พิกัดคลิก X/Y · จุดปลาย X2/Y2 (swipe) · ข้อความ/แปะ · รหัสปุ่ม (Keycode) · หน่วงหลังทำเสร็จ · คำอธิบายขั้นตอน — (**ควรซ่อนช่องที่ไม่เกี่ยวตามชนิด step**)
  3. ปุ่ม "เพิ่มขั้นตอน" (primary) · "อัปเดต" (warning)
  4. ปุ่ม "บันทึกพิกัดด้วยภาพ" (primary) · **"🧪 ทดสอบสเต็ปนี้ (แตะจริงบนจอ)"** (warning)
  5. แถวปุ่ม anchor: "📷 Anchor รอบจุดกด" · "🖼️ เลือกพื้นที่เอง" · "ลบ" · "ล้างฟอร์ม"

### 7.4 🎮 อื่นๆ (แมนนวล/Story) — มี **sub-tabs 3 หมวด**
- **🖱️ แมนนวล + ถ่ายจอ:** กล่อง 3 ช่อง (คลิกพิกัด X/Y + ส่งคลิก · พิมพ์ข้อความ + ส่ง · ปุ่มระบบ BACK/HOME/MENU) · กล่องถ่ายภาพหน้าจอ (ชื่อไฟล์ pattern + "ถ่ายทุกเครื่อง") + อ่านเพชร (💎 แคปจอ+อ่านเพชร Export JSON · ตั้งค่าพื้นที่เพชร · เติม id เว็บ)
- **🛠️ เครื่องมือ:** ADB Shell (ช่องคำสั่ง + รัน) · UI Inspector (ดู element บนจอ) · ดึงรายชื่อสมาชิกชมรม (ตั้งพื้นที่ชื่อ · ดึงรายชื่อ · วางรูป→อ่านชื่อ)
- **📖 Story Auto:** คำอธิบาย + "ความถี่เช็คจอ (วิ)" [2.5] + ปุ่ม "🎮 เริ่ม Story Auto"

### 7.5 ⚙️ ตั้งค่า — (scrollable, การ์ดหลายหมวด)
- **ไฟล์ระบบ:** ที่อยู่ไฟล์ ADB.exe [ช่องกรอก] + "บันทึกและโหลดใหม่" (จำถาวรลง adb_config.json)
- **พอร์ต ADB** · **Gemini API Key** (Story Auto AI fallback) · **ขีดจำกัดเครื่อง — รันได้กี่จอ** · **ตรวจสอบ Emulator** · **ตัวช่วยหาพิกัด** · **พิมพ์ภาษาไทย/Unicode (ADBKeyboard)**

---

## 8. Log panel (ล่างสุด, ยุบ/ขยายได้)
- หัวข้อ "คอนโซลบันทึกการทำงานของระบบ (Log Console)" + ปุ่ม "ล้างบันทึก" · "ย่อ/ขยาย"
- ข้อความมีเวลา `[HH:MM:SS]` + สี: info(ขาว/ฟ้า) · success(เขียว) · warning(ส้ม) · error(แดง) + emoji นำหน้า

---

## 9. Dialogs สำคัญ

### 9.1 🖼️ เลือกพื้นที่ Anchor (ตั้งภาพยืนยันหน้าจอก่อนกด)
- ซ้าย: Canvas แสดงภาพจอจริง — จุดแดง = ตำแหน่งกด, ลากกรอบเขียว = พื้นที่ภาพ anchor
- ขวา: เลือก Emulator · checkbox **"🎯 กดตรงที่เจอภาพนี้ (ตามปุ่มที่ขยับ)"** · "ถ้าไม่เจอภาพ:" [dropdown abort/skip/tap] · "รอสูงสุด(วิ)" · ปุ่ม จับภาพใหม่ / ทดสอบ match / บันทึก Anchor

### 9.2 🔀 ผังงานสคริปต์ (Flow View) — flowchart จริง
- แถบบน: label ขั้นที่เลือก + ปุ่ม "＋ แทรก" · ▲ · ▼ · "🗑 ลบ" · "🧪 ทดสอบขั้นนี้ (แตะจริงบนจอ)"
- Canvas ผัง (เลื่อนได้ 2 ทิศ): แคปซูลเขียว=เริ่ม/จบ · ข้าวหลามตัดส้ม=ขั้นมี anchor "เจอภาพ?" (เจอ↓ทำ, ไม่เจอ→แยก) · สี่เหลี่ยมน้ำเงิน=ขั้นปกติ · กล่องแดง=หยุด+รีเซ็ต · เส้น Yes/No · **คลิกกล่อง=เลือกขั้น (ไฮไลท์เหลือง + โหลดเข้าฟอร์มแก้)**
- อ้างอิงสไตล์ที่ผู้ใช้ต้องการ: ProcessOn flowchart (ข้าวหลามตัดเงื่อนไข + สี่เหลี่ยมกระบวนการ + Yes/No)

### 9.3 Dialogs อื่น
ตั้งค่าพอร์ต ADB · ตั้งพื้นที่เพชร (ครอปเลขเพชร) · ตั้งพื้นที่ชื่อชมรม · สร้างสคริปต์เร็ว · จัดการ Sets · สรุปผลการรัน (report หลังรันจบ)

---

## 10. States / พฤติกรรมสำคัญ
- **Idle:** ปุ่มรันใช้ได้ · หยุดปิด · การ์ดพร้อมรันบอกสถานะ
- **Running:** ปุ่มรันเปลี่ยนเป็น disabled/แสดงคิว · หยุดใช้ได้ · การ์ดความคืบหน้าต่อจออัปเดตสด · Log ไหล
- **Error/ติด:** จอที่ติด → บันทึก "รายงานปัญหา" อัตโนมัติ (ภาพหน้าจอ + บัญชี + สคริปต์ + ขั้นที่เท่าไหร่ + เหตุผล) ลง `error_reports/` แล้วรีเซ็ตเกม ไปรหัสถัดไป
- **Resume:** มี checkpoint รันต่อจากที่ค้างได้ · บัญชีมี last_status (เสร็จ/พลาด) โชว์เป็นจุดสี

---

## 11. เป้าหมายการออกแบบใหม่ (โจทย์ให้ดีไซเนอร์)
1. **ลูกค้าใช้ง่าย** โดยไม่เก่งคอม — flow ชัด: เช็คจอ/บัญชี → เลือกสคริปต์ → รัน → เห็นสถานะ
2. คงฟีเจอร์ครบทุกอย่าง (เป็นเครื่องมือ power-user ด้วย) แค่จัดวางให้ดี — แยกส่วน "ใช้งาน" กับ "สร้างสคริปต์/ตั้งค่า"
3. **ความคืบหน้าต่อจอแบบเรียลไทม์** ชัดเจน (จอไหน รหัสไหน ถึงขั้นไหน เสร็จ/พลาด) สำหรับรัน 10 จอ
4. **ผังงาน (flowchart)** อ่านง่าย เห็นทางแยกของเงื่อนไข (anchor) — เพิ่ม/ลบ/เทสสเต็ปได้
5. จุดที่ควรปรับ (ที่รู้แล้ว): ตัด/แปลไทย subtitle อังกฤษ · ตัดข้อความ status bar ที่ซ้ำ · tooltip ปุ่ม X ลบจอ · ซ่อนช่องฟอร์มที่ไม่เกี่ยวตามชนิด step

---

## 12. ข้อจำกัดทางเทคนิค (ที่ดีไซน์ต้องรู้)
- เป็น **Tkinter** (widget ชุดจำกัด) — เอฟเฟกต์ซับซ้อน (เงา/gradient/มุมมนจริง/animation) ทำยาก ควรออกแบบให้ flat
- Windows desktop app · หน้าจอเดียว ไม่มี routing แบบเว็บ · แสดงผลออฟไลน์
- ข้อความไทยทั้งหมด · ต้องรองรับหลายจอ (สูงสุด ~10) ในลิสต์/การ์ด

---
*สร้างจากซอร์สโค้ดจริง `gui.py` — ค่าสี/ปุ่ม/โครงสร้างตรงกับที่ใช้งานอยู่ ณ 2026-07-06*
