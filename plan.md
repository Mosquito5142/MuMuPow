# plan.md — Flow Editor แบบตัวต่อ (Visual Block Flow) สำหรับ MuMupow_new

> เป้าหมายรวม: ให้คนที่ไม่ใช่โปรแกรมเมอร์ "ต่อบล็อก" สร้างสคริปต์เองได้ทั้งเส้น —
> เห็นเป็นผัง แก้บนผังได้เลย ตั้งจุดกด/ภาพจากหน้าจอจริง และแตกทางเลือกได้จริง
> ("ถ้าเจอภาพนี้ → ทำชุดย่อยจนจบ → แล้วค่อยกลับมาต่อเส้นหลัก")

## สถานะความคืบหน้า (ติ๊กเมื่อจบเฟส)

- [x] เฟส 1 — รูปแบบข้อมูล `if_image` + ตัวรันรองรับ (macro_runner) — 2026-07-11 (+failure report, +ผลรายบัญชี last_status)
- [x] เฟส 2 — หน้าโฟลว์แบบดูอย่างเดียว (เรนเดอร์กิ่งซ้อนได้) — 2026-07-11 (webui/flow.js, toggle ลิสต์↔โฟลว์)
- [x] เฟส 3 — แก้ไขบนผัง: เพิ่ม/ลบ/ย้าย/แก้บล็อก + กิ่ง then/else — 2026-07-11 (path API: flow_add/update/delete/move)
- [x] เฟส 4 — ตั้งค่าจากภาพจอจริง — 2026-07-11 (คลิก=พิกัด, ลากกรอบ=ภาพ, ทดสอบ match, ตั้งค่ารอภาพ ป้ายไทย) ⚠ ยังไม่ได้ทดสอบกับ emulator จริง
- [x] เฟส 5 — ไฮไลต์สดตอนรัน + ทดสอบรันจากบล็อก — 2026-07-11 (step_path ใน progress + run_from) ⚠ ยังไม่ได้ทดสอบกับ emulator จริง
- [x] เฟส 6 — ระบบรอบข้างรองรับกิ่งซ้อน — 2026-07-11 (export scan recurse, run_set ในกิ่ง, gui.py แจ้งชัดเมื่อเจอ if_image, ปุ่มผังงาน→โหมดโฟลว์)
- [~] เฟส 7 — เก็บงาน — ตรวจความถูกต้อง (ขอบแดง+ข้อความไทย) ✅ · build exe ✅ · **ยังเหลือ: undo/redo, ซูม/แพน, ทัวร์ครั้งแรก, ทดสอบ end-to-end กับ emulator จริง**

**โบนัสนอกแผน (2026-07-11):** ระบบที่ webui ยังขาด เก็บครบแล้ว — ตั้งพื้นที่เพชร/ชื่อจากภาพจอ, ส่งเพชรขึ้นเว็บ (จับคู่ชื่อ+push), รายงานจุดที่ติด (ดูภาพได้), จัดการพรีเซ็ตพิกัด (เพิ่มจากภาพจอ/ลบ), ตั้งค่ารีเซ็ตเกมในหน้าตั้งค่า, เขียน last_status ลงบัญชีหลังรัน + สรุปยอด · **แก้บั๊กแคช:** script tags ใส่ `?v=` กัน WebView2 เสิร์ฟ JS เก่า (สาเหตุที่ผู้ใช้เห็น "ไม่ได้ทำ" ทั้งที่โค้ดใหม่แล้ว) — บัมพ์เลขทุกครั้งที่แก้ JS

---

## กติกาเหล็ก (ทุกเฟสต้องเคารพ)

1. **สคริปต์เก่า (flat array) ต้องรันได้เหมือนเดิม 100%** — `if_image` เป็นของ "เพิ่ม" ไม่ใช่ "แทน"
2. **ค่าในไฟล์ยังเป็นอังกฤษตามเดิม** (`abort/skip/tap`, `then/else`, ชื่อ type) — แปลไทยเฉพาะป้ายที่ตาเห็น
3. **ไม่แตะ `MuMupow.exe` (Tkinter)** ยกเว้นเฟส 6 (แค่ guard/handler เดียว) — เป็น fallback ของลูกค้า
4. **ห้ามเทสเมธอดเขียนไฟล์กับ `accounts.json` จริง** — copy + monkey-patch `_accounts_path` เสมอ
5. จบทุกเฟส: `python -m pytest tests/ -q` ต้องผ่านหมด + rebuild `MuMupow_web.spec` + เช็ค `Responding=True`
6. JS ฝั่งเว็บมี 2 ไฟล์: `webui/app.js` (หน้าหลัก) และ `webui/tools.js` (modal/เครื่องมือ) — ไฟล์ใหม่ของ flow editor ให้แยกเป็น `webui/flow.js` แล้วเพิ่ม `<script>` ใน index.html (spec bundle ทั้งโฟลเดอร์ `webui/` อยู่แล้ว)

---

## รูปแบบข้อมูลใหม่ (หัวใจของแผน — ตกลงก่อนเขียนโค้ด)

step ชนิดใหม่ `if_image` ที่ **ซ้อนขั้นย่อยข้างในได้**:

```json
{
  "type": "if_image",
  "desc": "ถ้ามีป๊อปอัพโฆษณา",
  "text": "ads_close.png",          // แบบที่ 1: ไฟล์ใน templates/
  "anchor_img": "<base64 png>",     // แบบที่ 2: ภาพที่ลากกรอบจากจอจริง (ใช้อันใดอันหนึ่ง)
  "threshold": 0.8,
  "timeout": 2.0,                   // รอสั้นๆ ก็ได้ (เหมือน skip-chain เดิม)
  "then": [                         // ✅ เจอ → ทำชุดนี้จนจบ แล้ว "กลับมาต่อเส้นหลัก" อัตโนมัติ
    {"type": "tap", "x": "880", "y": "40", "delay": 0.5, "desc": "กดปิดโฆษณา"}
  ],
  "else": []                        // (ไม่บังคับ) ไม่เจอ → ทำชุดนี้ก่อนไปต่อ
}
```

กติกาของชนิดนี้:
- ประเมินครั้งเดียวต่อการถึงบล็อก (แคปจอ → match → เลือกกิ่ง) — ไม่วนเอง
- ซ้อน `if_image` ข้างใน `then/else` ได้ จำกัดความลึก 5 ชั้น (กันวนไม่รู้จบ)
- `run_set` ใช้ใน `then/else` ได้ (ขยายตอนโหลดเหมือนเดิม)
- การอ้างตำแหน่งบล็อกใช้ "path" เช่น `[2]` = บล็อกที่ 3 ของเส้นหลัก, `[2,"then",0]` = บล็อกแรกในกิ่งเจอของบล็อกที่ 3

---

## เฟส 1 — ตัวรันรองรับ `if_image` (macro_runner.py)

**ทำอะไร**
- แยก loop ใน `execute_one` (บรรทัด ~322) ออกเป็น `_run_steps(device, account, steps, depth=0, path_prefix="")`
  ที่เรียกซ้ำตัวเองได้ — `execute_one` เหลือแค่เตรียม state แล้วเรียก `_run_steps(root)`
- เพิ่ม `_step_if_image`: แคปจอ → match (`find_image_in_bytes` ถ้าใช้ `text` / `match_template_bytes` ถ้าใช้ `anchor_img`)
  ภายใน `timeout` → เจอ = `_run_steps(then, depth+1)`, ไม่เจอ = `_run_steps(else, depth+1)` → คืนค่ากลับมาทำ step ถัดไปตามปกติ
- `_expand_sets` ต้อง recurse เข้า `then/else` ด้วย
- progress รายงานเป็น path string เช่น `"3.then.1"` (เตรียมไว้ให้เฟส 5 ไฮไลต์ได้)
- เคารพ `self.running()` ทุกชั้น (สั่งหยุดต้องหยุดถึงในกิ่ง)

**ไฟล์**: `macro_runner.py`, `tests/test_flow_runner.py` (ใหม่)

**ทดสอบ**: mock controller — (1) เจอภาพ → รัน then ครบแล้วกลับเส้นหลัก (2) ไม่เจอ → else/ข้าม
(3) ซ้อน 2 ชั้น (4) เกิน 5 ชั้น → log เตือน+ข้าม (5) สั่ง stop กลางกิ่ง → หยุดจริง (6) สคริปต์ flat เดิมผลลัพธ์เท่าเดิมเป๊ะ

**เสร็จเมื่อ**: เทสข้างบนผ่าน + pytest เดิม 128 ตัวผ่าน

---

## เฟส 2 — หน้าโฟลว์ ดูอย่างเดียว (webui/flow.js)

**ทำอะไร**
- backend: `Api.get_flow()` คืน tree ดิบทั้งก้อน (ทุก field รวม then/else) — แยกจาก `get_steps()` เดิมที่หน้า list ใช้อยู่
- สร้างแท็บย่อยบนหน้าสคริปต์: สลับ "มุมมองลิสต์" ↔ "มุมมองโฟลว์" (ลิสต์เดิมไม่แตะ)
- เรนเดอร์ด้วย DOM (ไม่ใช้ canvas): เส้นหลัก = คอลัมน์บล็อกแนวตั้ง (สไตล์เดียวกับ Flow View modal ที่มีอยู่),
  บล็อก `if_image` = ข้าวหลามตัด แตก 2 เลน (✅เจอ / ❌ไม่เจอ) วางคู่ขนานแล้ววาดเส้นรวมกลับ (merge) ลงบล็อกถัดไป
- กิ่งใช้เรนเดอร์ recursive ฟังก์ชันเดียว `renderChain(steps, path)`

**ไฟล์**: `web_app.py` (get_flow), `webui/flow.js` (ใหม่), `webui/index.html` (แท็บย่อย + script tag)

**ทดสอบ**: browser preview (DEMO data มีตัวอย่าง if_image ซ้อน 2 ชั้น) → screenshot ตรวจตาว่าเลนแตก/รวมถูก + ไม่มี console error

**เสร็จเมื่อ**: สคริปต์จริงที่มี if_image โชว์เป็นผังถูกต้อง, สคริปต์ flat เดิมโชว์เป็นเส้นตรงเหมือน Flow View เดิม

---

## เฟส 3 — แก้ไขบนผัง (path-based editing)

**ทำอะไร**
- backend เพิ่ม API แบบ path: `add_step_at(path, after_idx, type)`, `update_step_at(path, patch)`,
  `delete_step_at(path)`, `move_step_at(path, dir)` — ของเดิมแบบ idx คงไว้ให้หน้า list ใช้ต่อ
- บนผัง: จุด "+" บนเส้นเชื่อมทุกช่วง (รวมในกิ่ง) → popup เลือกชนิดบล็อก (18 ชนิด + if_image) → แทรกตรงนั้น
- คลิกบล็อก → ฟอร์มแก้ไขด้านขวา (reuse ฟอร์ม per-type ที่มีอยู่ + เพิ่มฟอร์มของ if_image: เลือกรูป/threshold/timeout)
- ปุ่มบนบล็อก: ลบ / ย้ายขึ้น-ลง (ในเลนเดียวกัน) / ใส่กิ่ง else (ถ้ายังไม่มี)
- ยังไม่ทำ drag-drop ในเฟสนี้ (ขึ้น-ลง + แทรกตรงจุด "+" พอใช้งานจริงได้ก่อน)

**ไฟล์**: `web_app.py`, `webui/flow.js`, `webui/index.html`

**ทดสอบ**: backend unit (path ops: แทรก/ลบ/ย้ายในกิ่งลึก 2 ชั้น + บันทึกแล้วโหลดกลับต้อง round-trip เป๊ะ),
preview: สร้างสคริปต์ "ถ้าเจอโฆษณา→ปิด แล้วไปกดรับของ" จากศูนย์ผ่านผังล้วนๆ

**เสร็จเมื่อ**: สร้าง/แก้/บันทึกสคริปต์ที่มีกิ่ง ได้ครบโดยไม่ต้องแตะมุมมองลิสต์เลย

---

## เฟส 4 — ตั้งค่าจากภาพจอจริง (จุดขายหลัก)

**ทำอะไร**
- ในฟอร์มแก้บล็อก เพิ่มปุ่ม "📸 ตั้งจากภาพจอ" → modal ภาพจอสด (reuse `Api.screenshot_b64()` ที่มีแล้ว):
  - บล็อก tap/swipe: **คลิกบนภาพ = ตั้ง x,y ให้เลย** (swipe = คลิก 2 จุด: ต้นทาง→ปลายทาง) + จุดแดงโชว์ตำแหน่ง
  - บล็อก if_image / anchor: **ลากกรอบ = ได้ `anchor_img` (base64) + region** (reuse logic ลากกรอบจาก `openGuildRegion` ใน tools.js)
- backend เพิ่ม `Api.test_anchor_match(b64, threshold)` → แคปจอปัจจุบัน + `match_template_bytes` → คืน {found, x, y, confidence}
  ให้ผู้ใช้กด "🔍 ทดสอบ" เห็นทันทีว่ากรอบที่ลากใช้ได้ไหม (เหมือนปุ่มทดสอบ match ในแอปเดิม)
- ป้ายทุกอย่างภาษาไทย (ตามบทเรียน dropdown abort/skip/tap)

**ไฟล์**: `web_app.py`, `webui/flow.js` (หรือแยก `webui/pick.js` ถ้าอยากให้ coord-picker ใช้ร่วมกับหน้า list ด้วย)

**ทดสอบ**: ต้องมี emulator จริงต่ออยู่ — คลิกตั้งพิกัด แล้วกด "ทดสอบสเต็ป" ให้จอขยับจริง; ลากกรอบ + ทดสอบ match ผ่าน

**เสร็จเมื่อ**: ตั้งบล็อก tap + if_image ได้ครบจากภาพจอโดยไม่ต้องพิมพ์ตัวเลขเอง

---

## เฟส 5 — เห็นสดตอนรัน + รันจากบล็อก

**ทำอะไร**
- `get_run_state()` ส่ง path ปัจจุบันของแต่ละจอ (จากเฟส 1) → ผังไฮไลต์บล็อกที่กำลังทำ (ขอบเขียวกระพริบ)
  ต่อจอที่เลือกดู (dropdown เลือกจอ) — ใช้ poll 1 วิ ที่มีอยู่แล้ว ไม่เพิ่มกลไกใหม่
- คลิกขวา (หรือปุ่มบนบล็อก) → "▶ ทดสอบรันจากบล็อกนี้ (จอแรกที่เลือก)" → backend `run_from(path)`:
  ตัด steps ตั้งแต่ path นั้นถึงจบเส้น (เฉพาะระดับ root ก่อน — ในกิ่งไว้ทีหลัง) รันเป็นรอบเดี่ยวไม่ผูกบัญชี
- แสดง badge "จุดที่ติดล่าสุด" บนบล็อก ถ้า run ก่อนหน้าล้มเหลวที่ path นั้น (อ่านจาก run state ตอนจบ)

**ไฟล์**: `macro_runner.py` (path ใน progress — ทำไว้แล้วเฟส 1), `web_app.py`, `webui/flow.js`

**ทดสอบ**: รันสคริปต์จริงกับ emulator → เห็นไฮไลต์วิ่งตามบล็อก; สั่ง run_from กลางเส้น → เริ่มถูกจุด

**เสร็จเมื่อ**: ดูผังแล้วรู้ทันทีว่าแต่ละจอถึงไหน/ติดตรงไหน

---

## เฟส 6 — ระบบรอบข้างรองรับกิ่งซ้อน

**ทำอะไร** (ไล่เก็บทุกจุดที่ scan/แปลง steps แบบ flat)
- `Api.export_profile` — `scan()` ต้อง recurse เข้า `then/else` (เก็บรูป/script sets ในกิ่งลง .mmpow ด้วย)
- `script_sets.expand_steps_with_sets` — recurse เข้า then/else (แก้ใน `script_sets.py` ระวัง cycle-detection ให้ครอบกิ่งด้วย)
- Flow View modal เดิมใน `tools.js` — ให้เรียก renderer ของ flow.js แทน (ลบโค้ดซ้ำ)
- มุมมองลิสต์ (`get_steps`) — แถว if_image โชว์เป็น "🔀 ทางเลือก (x บล็อกใน)" คลิกแล้วเด้งไปมุมมองโฟลว์
- **แอป Tkinter (แตะน้อยที่สุด)**: เพิ่ม `_step_if_image` ใน `gui.py` (`_get_step_handlers` ~7263) แบบ recursive เรียก handler เดิม
  — หรืออย่างน้อยที่สุด: guard log "สคริปต์นี้มีบล็อกทางเลือก ต้องรันด้วยโปรแกรมใหม่" กันรันแล้วเงียบหาย
- `MuMupow.spec` rebuild เฉพาะถ้าแตะ gui.py

**ทดสอบ**: export สคริปต์มีกิ่ง → import เครื่องเปล่า (โฟลเดอร์ temp) → ไฟล์ครบ; run_set ในกิ่งขยายถูก; pytest ทั้งหมดผ่าน

**เสร็จเมื่อ**: ไม่มีฟีเจอร์ไหน "ทำเป็นไม่เห็น" กิ่งซ้อนแล้วข้อมูลหาย

---

## เฟส 7 — เก็บงานให้ลูกค้าใช้จริง

- Undo/Redo ฝั่ง JS (snapshot JSON stack สูงสุด 50 ครั้ง, Ctrl+Z/Ctrl+Y)
- ตรวจความถูกต้องก่อนบันทึก: บล็อก if_image ไม่มีรูป, tap ไม่มีพิกัด → ขอบแดง + ข้อความไทยบอกว่าขาดอะไร
- ซูม/แพนผัง (CSS transform + ลากพื้นหลัง) สำหรับสคริปต์ยาว
- ทัวร์สั้นครั้งแรกที่เปิด (3 บับเบิล: "กด + เพื่อเพิ่มบล็อก / คลิกบล็อกเพื่อแก้ / ลากกรอบจากภาพจอ")
- rebuild `MuMupow_web.spec` + ทดสอบ end-to-end กับ emulator จริง 2 จอ + อัปเดต CLAUDE.md (รูปแบบ if_image + ไฟล์ flow.js)

---

## สิ่งที่ตั้งใจ "ยังไม่ทำ" (กันแผนบวม)

- drag-drop ลากบล็อกย้ายอิสระ (ใช้ +แทรก / ขึ้น-ลง ไปก่อน — ถ้าใช้แล้วยังอึดอัดค่อยเพิ่ม)
- loop block (`ทำซ้ำจนกว่า...`) — `clear_ads_loop` ครอบเคสหลักอยู่แล้ว ค่อยออกแบบทีหลัง
- เงื่อนไขจากข้อความ (`if_text`) — โครง if_image รองรับการเพิ่มชนิดนี้ภายหลังได้ทันที
- แก้ผังจากแอป Tkinter — Tkinter เป็นแค่ตัวรัน fallback

## ลำดับ build/verify มาตรฐาน (ใช้ท้ายทุกเฟส)

```powershell
python -m pytest tests/ -q
python -m PyInstaller --clean MuMupow_web.spec
# เปิด dist/MuMupow_new.exe → เช็ค Responding + webview procs → ปิด
```
